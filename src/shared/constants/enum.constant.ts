export enum Role {
  Admin = 'admin',
  User = 'user',
}

export enum StatusTask {
  ToDo = 'TO DO',
  InProgress = 'IN PROGRESS',
  Done = 'DONE',
}

export enum TypeTask {
  Task = 'Task',
  Bug = 'Bug',
  Story = 'Story',
}

export enum PriorityTask {
  Highest = 'Highest',
  Hight = 'Hight',
  Medium = 'Medium',
  Lowest = 'Lowest',
  Low = 'Low',
}
